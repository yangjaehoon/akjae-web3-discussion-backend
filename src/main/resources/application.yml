spring:
  application:
    name: akjae-web3-discussion-backend

  datasource:
    url: jdbc:mysql://localhost:3306/web3_discussion?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080

jwt:
  # 최소 256bit(32바이트) 이상의 Base64 인코딩된 시크릿 키 (운영 환경에서는 환경변수로 대체)
  secret: d2ViM2Rpc2N1c3Npb25fc2VjcmV0X2tleV9mb3JfandUX3Rva2VuX2dlbmVyYXRpb25fMjAyNl9ha2phZQ==
  expiration: 86400000  # 24시간 (ms)

# Auto-push hook test comment
